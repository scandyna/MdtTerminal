!include Mdt/SerialPort/PortInfo.puml

namespace Mdt.SerialPort {

  class Handle {
    + bool isValid()
    + nativeHandle()
    + {static} fromNativeHandle()
  }

  class Engine {
    + Handle open(PortInfo pi)
    + tryOpen(PortInfo pi)
  }

  Engine ..> Handle
  Engine ..> PortInfo

}
